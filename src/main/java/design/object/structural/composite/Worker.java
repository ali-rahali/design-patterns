package design.object.structural.composite;

/**
 * It's a leaf in compose design pattern definition
 */
public class Worker implements Person {

    @Override
    public void work() {
        // some work which is performed by Worker itself
    }
}
